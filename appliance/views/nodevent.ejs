<%- 
  deps
%>

head.js(     
  "<%- opts.host + "/socket.io/socket.io.js" %>",
  function() {
      var connect_url = "<%- opts.host + "/" + opts.namespace %>";
      var socket = io.connect(connect_url );
      NoDevent.setSocket( socket);
  });




      